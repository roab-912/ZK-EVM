[package]
name = "evm"
version = "0.0.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[features]
default = ["std"]
std = ["ruint/std"]

[dependencies]
ruint = { workspace = true }

[dev-dependencies]
revm = "40.0.3"
