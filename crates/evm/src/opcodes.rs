/// EVM opcodes.
///
/// Placeholder for v0.0 — real opcodes (STOP, PUSH1, ADD, ...) are added from
/// v0.1 onwards, along with byte decoding and PC-advance logic.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[non_exhaustive]
pub enum Opcode {}
